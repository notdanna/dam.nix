{ pkgs, ... }:

{
  # Paquetes adicionales para el servidor Linux
  # (DWM y Kitty movidos a módulos separados: dwm.nix y kitty.nix)

  # home.packages = with pkgs; [
  #   # Agrega paquetes aquí si es necesario
  # ];
}

